{
  "name": "logical-firefly",
  "version": "0.0.0",
  "private": true,
  "description": "LSL test framework — Vitest-style DSL for Linden Scripting Language",
  "type": "module",
  "scripts": {
    "gen": "tsx scripts/gen-stubs.ts",
    "build": "pnpm -r build",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "fast-xml-parser": "^4.5.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.28.2"
}
